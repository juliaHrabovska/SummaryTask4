var flag = true;
var image = document.getElementById("symbol");
var imgs = new Array('&uarr', '&darr'); //Добавте свои картинки через запятую
function imgsrc() {
    if (flag == true)
    image.src = imgs[i];
}